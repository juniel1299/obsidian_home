```linux


$ cd / 
$ cd pot/tomcat/apache-tomcat/webapps
ls - l 

sudo chmod -R 777 place 

root 폴더에서 
- u01 > app > oracle > product > 11.2.0 > xe > jdbc > lib > ojdbc6.jar 다운로드 


다시 webapps 밑에 place > web-inf > lib 안에 ojdbc8 을 지우고 6을 넣음


```
