# Flutter 구조 
1. Python과 동일하게 들여쓰기 문법 
2. React처럼 블록코딩 스타일
3. lib 폴더 내부의 main.dart 를 기준으로 시작함
4. 동작은 void main (){ } 에서 동작 
5. 화면에 대한 디자인은 Widget build(BuildContext context) 하단부 MaterialApp 에서 진행
## 메서드 
home 을 기준 파이썬식으로 작성 
-> 자동완성으로 다 찾아야할듯;

만약 감싸야한다 (ex: 정렬을 상위에 놓고 내부에 )